{% extends 'base.html.twig' %}

{% block body %}
    <h1>Paquet_coli</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paquet_Coli.id }}</td>
            </tr>
            <tr>
                <th>Villeexpedition</th>
                <td>{{ paquet_Coli.villeExpedition }}</td>
            </tr>
            <tr>
                <th>Villedestination</th>
                <td>{{ paquet_Coli.villeDestination }}</td>
            </tr>
            <tr>
                <th>Fraisenvoi</th>
                <td>{{ paquet_Coli.fraisEnvoi }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('colis_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('colis_edit', { 'id': paquet_Coli.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
